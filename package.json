{
  "name": "cortexium_platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "docusaurus": "docusaurus",
    "start:main": "cross-env BUILD_TARGET=main docusaurus start",
    "start:dev": "cross-env BUILD_TARGET=dev docusaurus start",
    "start:docs": "cross-env BUILD_TARGET=docs docusaurus start",
    "build:main": "cross-env BUILD_TARGET=main docusaurus build",
    "build:dev": "cross-env BUILD_TARGET=dev docusaurus build",
    "build:docs": "cross-env BUILD_TARGET=docs docusaurus build",
    "swizzle": "docusaurus swizzle",
    "deploy:main": "npm run build:main && wrangler deploy",
    "deploy:dev": "npm run build:dev && wrangler deploy",
    "deploy:docs": "npm run build:docs && wrangler deploy",
    "clear": "docusaurus clear",
    "serve": "docusaurus serve",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids",
    "typecheck": "tsc",
    "preview:main": "npm run build:main && wrangler dev",
    "preview:dev": "npm run build:dev && wrangler dev",
    "preview:docs": "npm run build:docs && wrangler dev",
    "deploy": "npm run build:main && wrangler deploy",
    "preview": "npm run build:main && wrangler dev"
  },
  "dependencies": {
    "@docusaurus/core": "3.10.1",
    "@docusaurus/faster": "3.10.1",
    "@docusaurus/preset-classic": "3.10.1",
    "@mdx-js/react": "^3.0.0",
    "clsx": "^2.0.0",
    "prism-react-renderer": "^2.3.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "3.10.1",
    "@docusaurus/tsconfig": "3.10.1",
    "@docusaurus/types": "3.10.1",
    "@types/react": "^19.0.0",
    "cross-env": "^10.1.0",
    "typescript": "~6.0.2",
    "wrangler": "^4.90.0"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 3 chrome version",
      "last 3 firefox version",
      "last 5 safari version"
    ]
  },
  "engines": {
    "node": ">=20.0"
  }
}
